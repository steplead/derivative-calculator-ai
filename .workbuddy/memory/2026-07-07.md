# 2026-07-07 工作日志

## Phase 1.7 P0 API Security Patch — 完成并部署上线

3 个 commit 合并到 main：
- `af213c3`: fix(security): P0 API security patch — close Phase 1.6 R1-R4
  - security.ts: +UA blacklist, +empty UA rejection, 更新文档头
  - middleware.ts: 所有错误响应加 Cache-Control: no-store
  - 5个API route: 成功响应改 Cache-Control: private, no-store, 错误响应加 no-store
- `c18228d`: fix(security): add /api/* Cache-Control: private, no-store to _headers
  - 发现 _headers 对动态 Workers Function 路由不生效
- `4466e84`: fix(security): pass Cache-Control in NextResponse.json constructor, not .headers.set()
  - 发现 response.headers.set() 被 CF Pages 平台层覆盖为 private, max-age=14400
  - 改为在构造函数中传 headers 参数

线上验证结论：
- 裸curl/curl UA/python UA/空UA → 全部403 ✅
- 403 响应: Cache-Control: no-store, cf-cache-status: BYPASS ✅
- 浏览器 UA → API 200 ✅
- 200 响应: CF Pages 平台层覆盖 Cache-Control 为 private, max-age=14400
  - private = 禁止 CDN 缓存 → R1 核心风险（CF 边缘缓存绕过安全层）已实际缓解
  - max-age=14400 = 浏览器本地缓存 4 小时 → 低风险（同一用户、同表达式）
  - 如需完全 no-store，需配置 Cloudflare Transform Rule（Modify Response Header）

风险状态：
- R1 HIGH → ❌ **未缓解！** 线上验证确认 `private` 指令不被 CF Pages 尊重，API 200 仍被 CDN 缓存（cf-cache-status: HIT, Age 递增）
- R2 HIGH → 完全修复（security.ts 内 ABUSE_UA_PATTERNS 覆盖 API）
- R3 HIGH → 完全修复（security.ts 内空 UA → 403）
- R4 MEDIUM → 400/403/405 响应已修复（no-store 被 CF 尊重），200 响应被 CF 覆盖为 max-age=14400
- R5 MEDIUM → 已文档化，不做代码改动

## Phase 1.7 缓存验证补完（21:30）

**关键发现**: CF Pages 对 API 200 响应的 `private` 指令无效，CDN 缓存确认：
- sin(x) URL: MISS→HIT→HIT，Age 52→57→600+
- cos(x) URL: MISS(55s)→HIT(5s)
- 缓存时长 4 小时，绕过所有安全层（middleware/rate limit/UA 黑名单）

CF Pages 行为差异：
- 200 成功响应: Cache-Control 被覆盖为 `private, max-age=14400` → HIT
- 400/403 错误响应: `no-store` 被尊重 → BYPASS
- 405 响应: DYNAMIC

**修复方案**: 需在 Cloudflare Dashboard 配置 Cache Rules（/api/* → Bypass Cache），代码层无法解决

## Phase 1.8 — Cloudflare Cache Rule 配置与线上验证（完成）

- 用户手动在 Cloudflare Dashboard 配置 Cache Rule: "API bypass cache"
  - Expression: `http.host eq "derivativecalculatorai.com" and starts_with(http.request.uri.path, "/api/")`
  - Action: Bypass cache, Active, Order 1
- 线上验证 5 项测试全部 PASS：
  - API 200: cf-cache-status=DYNAMIC, 无 Age, 每次到源站（15-17s）
  - curl UA 403: DYNAMIC, no-store ✅
  - 空 UA 403: DYNAMIC, no-store ✅
  - 400: DYNAMIC, no-store ✅
  - 405: DYNAMIC ✅
  - 正常页面全部 200，不受影响
- **R1 CLOSED**: CDN 不再缓存 API 响应，安全层 100% 生效
- 可转入 Phase 5 Monitoring
