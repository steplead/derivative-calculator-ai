# 2026-07-09 工作日志

## Phase 5B-lite: External Monitoring + Alerting + Runbook

### 完成内容
1. **线上基线检查**: 10 项全部 PASS。首页 200 (HIT)，子页面 200，API 200 (DYNAMIC)，admin/health 401 (fail-closed)，unblock-ip 410，400/405 不缓存。CF Cache Rule "API bypass cache" 正常工作。
2. **监控方案对比**: UptimeRobot 免费版 vs CF Workers Cron vs 本地 Bash 脚本。推荐 A+C 组合：UptimeRobot 监控首页 always-on，本地脚本做深度检查。
3. **健康检查脚本**: `scripts/health-check.sh` — 10 项检查，支持 table/JSON 输出，支持 webhook 告警。每次请求间隔 2 秒避免 rate limit，API timeout 30 秒 (OpenRouter 耗时 15-20 秒)。无 ADMIN_MONITORING_TOKEN 时 admin/health 检查期望 401。
4. **Runbook**: `PHASE_5B_LITE_MONITORING.md` — 覆盖 8 个故障场景 (首页宕机/API 500/403 误封/429/Cache Rule/OpenRouter/D1/Redis)，含排查步骤和修复方法。

### 关键发现
- API 求导请求 origin latency ~16 秒 (server-timing: cfOrigin;dur=16098)，health-check 脚本 timeout 必须设 30 秒
- UptimeRobot 免费版不支持自定义 UA/Header，只能监控首页 200 (首页 CDN 缓存 HIT，不受 abuse detection 影响)
- abuse detection 会拦截 curl UA (403)，但首页因 CDN 缓存返回 200

### 产出文件
- `scripts/health-check.sh` — 可执行健康检查脚本
- `PHASE_5B_LITE_MONITORING.md` — 监控方案 + Runbook 完整文档

### Phase 5B-lite 正式落地 (后续)
5. **整理 docs/MONITORING.md**: 从 `PHASE_5B_LITE_MONITORING.md` 整理，补充 24/48/72 小时观察清单。
6. **Commit & Push**: `7546eed` — `chore: add uptime checks and monitoring runbook` (2 files: docs/MONITORING.md, scripts/health-check.sh)，含 `--noproxy '*'` 修复。
7. **部署后验证**: health-check.sh 10/10 PASS, 手动验证 5/5 PASS (API DYNAMIC, 首页 200, 导数 200, admin/health 401, unblock-ip 410)。
8. **完整项目复盘**: 主站为 Production MVP，过去 4 周 18 commits 100% 修复/安全/监控，产品功能零增长。推荐选项 A（维护模式）。

### 约束遵守
- 未做 D1 schema migration、未清理 Python API、未改 SEO/sitemap、未碰 Chrome Extension
- 未引入外部监控平台、未记录原始 IP/UA、未增加每请求 D1 写入
- 未提交 public/sitemap.xml、.workbuddy/、AUDIT_REPORT、根目录临时分析 md、legacy Python API
- Phase 5B-lite 正式落地完成，项目进入维护观察阶段

### 产品功能复盘 (09:45)
9. **只读功能复盘**: 5 个 calculator 只有 derivative 是产品级质量（完整 AI prompt + 3x retry + Redis cache + 校验）。integral/limit/ODE 的 AI 是缩水版（300 tokens, 5s timeout, 无校验）。Matrix 无 AI，UI 异类。Wiki 14 篇有真实内容，Problems/Directory/Practice 是同数据的三视图。Chrome Extension 121MB 未发布应冻结。
10. **产品方向结论**:
    - Core: derivative calculator + AI explanation + Wiki
    - Freeze: ODE, Matrix, Chrome Extension, Practice, Directory
    - Improve next: integral AI prompt 质量对齐 derivative
    - Don't do: 新增 calculator、扩展 Wiki、新增 Problems、发布 Extension
11. **核心矛盾**: 5 个 calculator 中只有 1 个产品级质量。用户遇到差体验不会怪功能不成熟，只会觉得整个网站不行。
