
## SEO Phase 0 只读审计（derivative-seo-v2）
- 全站 URL inventory：3137 题目页（可索引 2350），可索引 derivative 仅 504（<integral 941/limit 905）
- **决定性发现**：`/[slug]` 页 SSR 主体空洞——答案靠 Calculator.tsx（'use client'）客户端 JS fetch 渲染，Googlebot 抓不到真实答案和步骤
- **软404被缓存**：不存在的 slug 返回 HTTP 200 + catch 块兜底 UI，失败结果 cf-cache-status HIT 缓存 4 小时
- **ln 数学 bug**：nerdamer 把 ln(x) 当未知符号，diff(ln(x),x)=ln（应为 1/x），影响 44 页
- problems 页 SSR 空（依赖 D1 API limit≤100），directory 页正常（problems.json fallback，1084 链接）
- sitemap.xml 仅 9 URL，覆盖率 0.38%
- 100 页抽查：0 GREEN / 0 YELLOW / 100 RED（SSR 空洞是自动失败项）
- 最终决策：NO-GO（修 SSR 预渲染 + ln bug + 软404 后转 CONDITIONAL GO）
- 交付：audit-output/derivative-seo-v2/ 11 个文件
