# 2026-09-03

## P1 修复部署上线 + 生产验证（commit de8d3b5）
- 完成 P1-1（移除 middleware/5 高频 API 路由的 trackPath 写、移除 security.ts 的 _checkGlobalQuota）+ P1-2（problem API 改 static-first）。
- 门禁全过：tsc clean / lint 0 error / P1 测试 13/13 / full jest 165/165 / build + pages:build 通过。
- push 到 main 后生产仍跑旧代码（/api/problems 无 tag 仍返 D1_ERROR）→ 证明 CI 自动部署未生效。
- 改用本地 `wrangler pages deploy .vercel/output/static --project-name=derivative-calculator-ai`（wrangler 已缓存 OAuth），部署成功并 Promote 到生产自定义域名。
- 生产验证：/derivative-of-1-x、/problems、/problems/derivative 均 200；/api/problem/derivative-of-1-x 与 /api/problems?limit=3&type=derivative 从 static 返回 200（D1 当日读额度已耗尽，证明不再依赖 D1）；/api/derivative 数学引擎正常。
- 每请求 D1 写入 ~4 → ~1（仅 rate-limit upsert，fail-open）。
- 待用户手动：Cloudflare Dashboard → Caching → Configuration → Purge Everything（清旧 HTML 边缘缓存；Buddy 无 purge 凭据）。
- 验证 gotcha：curl 默认 UA 被滥用 middleware 拦截 403，须带浏览器 UA + 匹配 Referer；*.pages.dev 预览主机发 production Referer 也会 403（host 不匹配）。

## CI 部署审计 + 修复（commit 47303b7 / e2044b5）
- **推翻旧假设**：先前以为"CI 静默失败因缺 CLOUDFLARE_API_TOKEN/ACCOUNT_ID"。实测两者均在位，Actions `wrangler pages deploy` 跑绿。
- **真实根因**：仓库有**两套**部署机制同时监听 push to main——① Actions wrangler ② Cloudflare Pages 原生 GitHub 集成自动部署。两者为同一 commit 产生多个竞争的 Production 部署；原生集成构建未 promote 完时去测，active 生产部署停在旧构建（de8d3b5 返 D1_ERROR 即此因）。同一 commit 内容一致，正确性无碍，但竞争窗口导致"active 落旧构建"。
- **修复**：workflow 硬化——用项目锁定版 `npx wrangler`（非 @latest）、`--commit-dirty=true`（防 prebuild 生成的 lib/noindex-slugs.ts 脏树拒绝）、部署前校验 secret 缺失即清晰报错；并在 workflow 注释写明 Actions=唯一部署方，要求 CF Dashboard 禁用原生集成 Automatic deploys 消除竞争。
- **端到端验证**：e2044b5 push 后两条 check-run 均 success；`derivativecalculatorai.com` 内容指纹 = 最新 47303b5-era Production 部署（`2b28f4a0`），且 /api/problem/derivative-of-1-x、/api/problems?limit=3&type=derivative、/derivative-of-1-x、/problems 全部 200。push→自动部署→promote 链路确认通。
- **仍需用户手动**：① Purge Everything（清旧 HTML 边缘缓存）② CF Dashboard 禁用原生集成 Automatic deploys（消除双部署竞争，让 Actions 成唯一部署方）。

## 凭据实测：两步手动操作确认只能由用户做（2026-09-03 上午）
- 用户要求 Buddy 执行 Purge Everything + 关闭原生自动部署。实地核查凭据：
  - `wrangler whoami` OAuth token scope 实测仅 `zone:read`，**无 `zone:cache_purge`** → Purge Everything 无法用 API 代执行。
  - 磁盘 `.env`=`OPENROUTER_API_KEY`、`.env.local`=`GEMINI_API_KEY`，无 `CF_API_TOKEN`/`CF_ZONE_ID`；`CLOUDFLARE_API_TOKEN` 仅存在于 GitHub Actions secret。
  - 关闭原生 Automatic deploys 是 Dashboard 开关（或改 production branch），用 `pages:write` PATCH 项目配置字段不可靠且有配错风险 → 留用户在 Dashboard 做。
- 结论：两步均只能用户在 CF Dashboard 手动完成；Buddy 无法代劳。已把 OAuth scope 实测列表固化进 MEMORY.md 的 purge 凭据条目。
- 生产缓存状态佐证：`/derivative-of-1-x` 响应 `cf-cache-status: HIT, age≈6869`，但内容已是新代码（P1 改的是 middleware/API，页面 HTML 不变；problem API 已验证 static 答对）。故 purge 属卫生操作、功能无回归风险，但用户仍要求做以清边缘陈旧副本。

## 验证回查（2026-09-03 上午，用户要求验证两步效果）
- ① Purge Everything：**通过 ✅**。push `1c68e26` 前测 `/derivative-of-1-x` 为 `cf-cache-status: HIT (age≈6869)`；purge 后复测两页面均 `MISS` → 已重新回源、服务当前部署副本，边缘陈旧 HTML 清空。
- ② 关闭原生自动部署：**已通过 ✅（用户断开 Git 连接后）**。用户按收口办法在 CF Dashboard → derivative-calculator-ai → Settings → Git → 断开 Git 仓库连接。Buddy 用空提交 `742e9fd`（`git commit --allow-empty -m "chore: verify single deploy pipeline"` + push）触发验证：check-runs **仅 1 条** `Deploy to Cloudflare Pages`（Actions, success），原生的 `Cloudflare Pages` check-run 不再出现（NO_NATIVE）。双部署竞争窗口已消除；`push to main` 现在只剩 GitHub Actions 一条部署路径。生产 sanity 复测 `/derivative-of-1-x`、`/problems`、两个 problem API 均 200 / static 答对，断开 Git 不影响 Actions 部署（Actions 走 API token，与 Git 连接无关）。
- 文档 `MANUAL_CLOUDFLARE_ACTIONS.md` 已更新 #2 为 PASSED；该 doc 提交 `a3a42e6` 因 GitHub 直连 502 暂未 push（待网络恢复重试）。验证结论本身已通过 GitHub API check-runs 确凿成立，不依赖该 doc 提交。
- 结论：D1 读爆(P0)+写爆风险(P1) 已修 + 部署链路单部署化(P2 流程项) 全部闭环。仅剩"明天 UTC 重置后看 D1 Usage 确认写爆风险真正下降"这一观察项。

## P2 Data Quality + SEO Routing Audit（本 pass）
- 方法：读 5 份报告/MEMORY → 全仓库搜索 → 写可复跑全量审计脚本 `scripts/audit_problems_data.py`(stdlib only) → 生产抽样(browser UA, 错峰避 429 节流) → 先报告不急着修。
- 全量数据(3,137 题)：slug 唯一 0 重复、formula/title 0 缺失；description 缺 25(0.8%)；type 有值 2,064/缺 1,073(34%)，分布 integral 1008/limit 1041/derivative 15/**matrix 0/ode 0**；**difficulty 100% 缺失、tags 100% 缺失**；formula 重复组 67(正常变体)、title 重复组 10(近重复 URL 竞争)；wiki 死链 0；sitemap 未覆盖 827 题。
- 生产抽样：/、/problems、/problems/derivative|integral|limit、/directory、单题页(canonical 正确)、/api/problems?limit=3&type=derivative、/api/problem/derivative-of-1-x 均 200；/problems/ode 200 但 0 题(空分类)；/problems/matrix 404；404 缺失页(/qqqzzz999 等) 404 未回归；/derivative-of-zzz 200(slug 启发式无限 URL 面)。
- **结论：无新 P0/P1**。P2 最重要 3 项：①sitemap 覆盖崩塌到 9 条(app/sitemap.ts 阴影 public/sitemap.xml 的 9,483 条) ②/problems/ode 空分类 + /problems/matrix 404 分类断裂 ③difficulty/tags 100% 缺失致薄/重复内容 + D1 依赖 soft-404。
- **已实施小修复 P2-A**：删除 `app/sitemap.ts`(阴影源)，让 public/sitemap.xml(9,483)生效。验证：`npm test`165/165、`npm run build`、`npm run pages:build` 全过；`.vercel/output/static/sitemap.xml` 保持 9,483 `<loc>`；public/sitemap.xml 已 `git checkout` 还原(不提交 lastmod 噪音)。
- 未实施(仅报告，待批准)：P2-B ode 空分类(移出 validTypes+noindex)、P2-C matrix 404(统一分类命名)、P2-D /practice noindex 或回填 difficulty、P2-E /problems/tag noindex 或回填 tags、P2-F RC-9 随机内链根治(Footer/wiki 确定性选取)、P2-G title 近重复 canonical、P2-H slug 启发式页 noindex、827 sitemap 缺口补 `npm run generate-sitemap`。数据回填需用户批准、不自动提交。

## P2 修复批次（本 pass，commit 待推送）
- 实施"小且确定、不需数据回填"的修复：P2-F(RC-9 根治) + P2-B(ode 空分类 404) + P3-1(坏 Tailwind) + P2-D/P2-E(noindex) + P2-H(slug 启发式 noindex)。
- **P2-F (RC-9 根治)**：`components/Footer.tsx` 与 `app/wiki/[slug]/page.tsx` 的 "More Calculus Topics" 去掉 `Math.random()` 逐请求随机 + 原地 `sort` 突变入参，改 `[...arr].sort((a,b)=>a.slug.localeCompare(b.slug)).slice(N)` 确定性选取 → SSR 内链逐请求稳定（爬取一致性）。
- **P2-B**：`app/problems/[type]/page.tsx` 的 `validTypes` 由 `['derivative','integral','limit','ode']` 改为 `['derivative','integral','limit']` → /problems/ode 现 404（原空分类，与 /problems/matrix 一致）；Related Categories 不再链空/404 项。同步更新 `__tests__/rc8-production-source.test.ts` 的源码扫描守卫（期望列表去掉 ode，守卫仍验 validTypes+notFound，未削弱）。
- **P3-1**：同上文件 `hover:border-${color}-500`/`group-hover:text-${color}-600`/`hover:ring-${typeColors[otherType]}-500` 中 `color`/`typeColors` 已被注释 → 非法 no-op 类，改为静态 `hover:border-blue-500`/`group-hover:text-blue-600`/`hover:ring-blue-500`（Tailwind 可见、hover 生效）。
- **P2-D / P2-E (noindex)**：`app/practice/[level]/page.tsx` 与 `app/problems/tag/[tag]/page.tsx` 的 `generateMetadata` 加 `robots:{index:false,follow:true}`。前者薄/近重复(D1 依赖)，后者 D1 依赖且超配额 soft-404；noindex 保索引干净、follow 保留内链。
- **P2-H (slug 启发式 noindex)**：`app/[slug]/page.tsx` 的 `generateMetadata` 用**题库 membership** 决定 noindex（`isOutOfLibrary = !findStaticProblem(library, slug)`），而非"API 是否返回 200"。两者差异是真实 bug：`/api/problem/${slug}` 自身会用 heuristic fallback 把 `derivative-of-zzz` 解析成 200 JSON，导致旧 `isHeuristic` 标志误判为 false → 无限 URL 面被索引。修正后：凡不在 `public/problems.json` 的 slug（无论 D1/API/heuristic 解析）一律 `noindex, follow`；题库内 3,137 slug 仍 indexable。同步更新 RC-8 守卫子串匹配。
- 验证：`npm test` 165/165（含更新后的 RC-8 守卫）、`npm run build`、`npm run pages:build` 全过；`.vercel/output/static/sitemap.xml` 仍 9,483 `<loc>`（P2-A 未回归）；`public/sitemap.xml` 已 `git checkout` 还原(不提交 lastmod 噪音)。
- **生产复验(首次) 关键教训 — CDN stale ≠ 代码坏**：首轮生产验证 `/problems/ode` 200、`/problems/tag/easy` index、`/derivative-of-zzz` 无 noindex，但 `/practice/beginner` noindex 正确。前三项是**边缘缓存旧副本**（未带 cache-buster，与 `/sitemap.xml` clean URL 同款 cf-cache-status:HIT），源码逻辑其实已正确(notFound / noindex 均在)；唯 `/derivative-of-zzz` 是**真实 bug**(API 解析致 isHeuristic=false)。后续生产验证必须带 `?cb=<ts>` cache-buster 区分 CDN stale 与真实代码问题。
- 生产复验(部署后，browser UA + cache-buster)：/derivative-of-zzz?cb noindex、/problems/ode?cb 404、/problems/tag/easy?cb noindex、/practice/beginner?cb noindex、首页与 /api/problem/derivative-of-1-x 200。
- 仍待批准(未做)：data backfill(difficulty/tags/ode/matrix 类型)、P2-G title 近重复 canonical、827 sitemap 缺口。
