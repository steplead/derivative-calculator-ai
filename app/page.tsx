export const runtime = 'edge';

export default function Page() {
    return (
        <div>
            <h2>Page Loaded (Hello World)</h2>
            <p>If you see this, the Cloudflare Worker is running!</p>
        </div>
    )
}
