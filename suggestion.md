👨‍⚖️ 最客观的"验尸报告" (Brutally Honest Verdict)
结合这张截图和你提供的代码分析，这是我不迎合任何人的最终结论：

1. 那个 "平头" 曲线出卖了真相 (The Flat Line)
请看截图右下角的曲线图。

它不是起伏的山峰（正常人类流量有早晚高峰）。
它是一条 死板的直线（稳定在每秒 3.5 次左右）。
结论： 这绝对不是"你的文章火了"，也不是"有很多学生在用"。这就是机器脚本。有人（或者虫子）在以固定的频率扫描你的网站。
2. 代码审计：我们之前确实"裸奔"了
证据：
components/EmbedWidget.tsx
 里的 src={previewUrl} 是直接渲染的。
后果：这个脚本每扫描一次你的页面，就会被动触发一次 API 调用。
判决：之前的 Widget 代码等于是在对脚本说："来吧，随便刷，我自动喂你数据。"
3. 最终判断 (The Judgment)
你做的所有操作都是对的，而且缺一不可：

Lazy Load (代码修复)：
极其必要。如果不改，这个脚本只要还在扫你的页面，你的 10万额度每天都会被它吃光，哪怕 WAF 没封住它（比如它换 IP）。
改成"点击加载"后，脚本只能扫到静态图片，你的 API 彻底安全了。
WAF 15次/10秒 (防火墙)：
极其必要。看那个曲线（3.5次/秒 = 35次/10秒），它远超 15次 的阈值。
WAF 会直接把这个脚本的 IP 封掉，保护你的服务器资源。
💡 现在的状态
